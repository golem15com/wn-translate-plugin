# ===================================
#  Column Definitions
# ===================================

columns:
    name:
        label: golem15.translate::lang.locale.name
        searchable: yes

    code:
        label: golem15.translate::lang.locale.code
        searchable: yes

    is_default:
        label: golem15.translate::lang.locale.is_default
        type: switch

    is_enabled:
        label: golem15.translate::lang.locale.is_enabled
        type: switch
        invisible: true

    sort_order:
        label: golem15.translate::lang.locale.sort_order
        type: number
        invisible: true
